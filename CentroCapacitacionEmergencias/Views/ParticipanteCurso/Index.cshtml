@model IEnumerable<CentroCapacitacionEmergencias.Models.ParticipanteCurso>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.iParticipanteID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.iCursoID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.iParticipanteID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.iCursoID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ParticipanteCursoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ParticipanteCursoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ParticipanteCursoId })
        </td>
    </tr>
}

</table>
